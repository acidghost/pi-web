{
  "scripts": {
    "dev": "NODE_ENV=development bun --watch app/server.ts",
    "start": "bun run build && ./dist/pi-web",
    "server:src": "bun run app/server.ts",
    "build": "bun run scripts/build.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@types/bun": "latest",
    "typescript": "6.0.3"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.74.0",
    "@earendil-works/pi-ai": "^0.74.0",
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "dompurify": "^3.4.2",
    "lit": "^3.3.2",
    "marked": "^18.0.3",
    "missing.css": "^1.3.0",
    "shiki": "^4.0.2",
    "zod": "^4.4.3"
  }
}
